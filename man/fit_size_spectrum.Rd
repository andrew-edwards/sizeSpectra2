% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-size-spectrum-mlebin.R,
%   R/fit-size-spectrum.R, R/fit-size-spectrum.data.frame.R,
%   R/fit-size-spectrum.numeric.R
\name{fit_size_spectrum_mlebin}
\alias{fit_size_spectrum_mlebin}
\alias{fit_size_spectrum}
\alias{fit_size_spectrum.data.frame}
\alias{fit_size_spectrum.numeric}
\title{Fit a size spectrum to data using maximum likelihood}
\usage{
fit_size_spectrum_mlebin(
  dat,
  x_min = NULL,
  x_max = NULL,
  b_start = -1.9,
  b_vec = NULL,
  b_vec_inc = 1e-05
)

fit_size_spectrum(dat, ...)

\method{fit_size_spectrum}{data.frame}(dat, strata = NULL, ...)

\method{fit_size_spectrum}{numeric}(
  dat,
  x_min = NULL,
  x_max = NULL,
  b_vec = NULL,
  b_vec_inc = 1e-05,
  b_start = NULL
)
}
\arguments{
\item{dat}{One of:
\itemize{
\item \code{numeric} vector of values (such as individual body masses or lengths), which uses
the MLE method (via the function \code{\link[=fit_size_spectrum.numeric]{fit_size_spectrum.numeric()}};
\item \code{data.frame} of count data for the MLEbin method, where each row represents a
bin. At a minimum this has to include the columns:
\itemize{
\item \code{bin_min}
\item \code{bin_max}
\item \code{bin_count}.
The values \code{bin_min} and \code{bin_max} in each row correspond to the min and max
bounds of that bin, with \code{bin_count} being the count of individuals in that bin.
}
}}

\item{x_min}{minimum value of data to fit the PLB distribution to. If \code{NULL}
(the default) then it is set to the minimum value of the data (if \code{dat} is
\code{numeric}), else to the minimum bin break of the lowest bin. If not \code{NULL}
then the fitting is restricted to values greater than or equal to
\code{x_min}; for the MLEbin method this is the first full bin equal to or above \code{x_min}
(i.e. first bin with \code{bin_min >= x_min}). Similarly for \code{x_max} (fitting
is restricted to including the largest bin for which \code{bin_max <= x_max}).}

\item{x_max}{maximum value of data to fit the PLB distribution to. If \code{NULL}
(the default) then it is set to the maximum value of the data (if \code{dat} is
\code{numeric}), else to the maximum bin break of the highest bin
If not \code{NULL} then the fitting is restricted to values below or equal to
\code{x_max}; for the MLEbin method this includes the highest bin for which
\code{bin_max <= x_max}.}

\item{b_start}{for the MLEbin method, the starting estimate for numerical
search for the MLE, since there is no analytical value.}

\item{b_vec}{vector of values for the confidence interval calculation, to be
used as the \code{vec} argument of \code{calc_mle_conf()}}

\item{b_vec_inc}{increment value for the confidence interval calculation, to be
used as the \code{vec_inc} argument of \code{calc_mle_conf()}}
}
\value{
\itemize{
\item If \code{dat} is numeric then returns a list object of class
\code{size_spectrum_numeric} (such that we can plot it
with \code{\link[=plot.size_spectrum_numeric]{plot.size_spectrum_numeric()}}, with objects
\itemize{
\item \code{b_mle} maximum likelihood estimate of $b$
\item \code{b_conf} vector giving 95\% confidence interval for $b$
\item \code{x} vector of original values of \code{dat}
\item \code{x_min} the \code{x_min} value used for the fitting
\item \code{x_max} the \code{x_max} value used for the fitting
\item \code{method} to describe the fitting method used, in this case \code{MLE}
}
\item If \code{dat} is a data.frame then returns a list object of class
\code{size_spectrum_mlebin} with same objects as above, except for
\itemize{
\item \code{data} but instead of \code{x} it is the original \code{data.frame}, arranged by
the increasing values of \code{bin_min}, and also has the columns
\itemize{
\item \code{count_gte_bin_min} total count of values in bins for which \code{bin_min}
\eqn{\geq} the value of \code{bin_min} for this row
\item \code{low_count} total count of values in bins for which \code{bin_min}
\eqn{\geq} the value of \code{bin_max} for this row, so the lowest possible count of
values above this bin
\item \code{high_count} total count of values in bins for which \code{bin_max}
\eqn{\geq} the value of \code{bin_min} for this row, so the highest possible count of
values above this bin; for non-overlapping bins will be the same as
\code{count_gte_bin_min} (but is needed for plotting).
Note that if \code{x_min} and/or \code{x_max}
are prescribed such that some data are not included in the fit (e.g. \code{x_min}
is greater than the \code{bin_max} of the smallest bin), then they are omitted in \code{data}.
}
}
}
}
\description{
The function automatically uses the appropriate method depending on the data
class. For a simple \code{numeric} vector, the MLE method will be used. For a
\code{data.frame} with the appropriate columns (see below) the MLEbin method will
be used. To use the MLEbins method for species-specific bins, please use
\code{\link[=fit_size_spectrum_mlebins]{fit_size_spectrum_mlebins()}}. See appropriate vignettes.
}
\examples{
\dontrun{
res_vec <- fit_size_spectrum(sim_vec)
plot(res_vec)

x_binned <- bin_data(res_vec$x, bin_width = "2k")`
res_mlebin <- fit_size_spectrum(x_binned)
plot(res_mlebin)
# See the vignettes for further details and refinements.
}
}
\author{
Andrew Edwards
}
