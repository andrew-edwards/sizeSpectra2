% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-size-spectrum-mlebins.R
\name{fit_size_spectrum_mlebins}
\alias{fit_size_spectrum_mlebins}
\title{Fit a size spectrum to data using the MLEbins method.}
\usage{
fit_size_spectrum_mlebins(
  dat,
  x_min = NULL,
  x_max = NULL,
  b_start = -1.9,
  b_vec = NULL,
  b_vec_inc = 1e-05
)
}
\arguments{
\item{dat}{\code{data.frame} of the data to be fit using MLEbins, where each row represents a
bin. At a minimum this has to include the columns:
\itemize{
\item \code{bin_min}
\item \code{bin_max}
\item \code{bin_count}.
The values \code{bin_min} and \code{bin_max} in each row correspond to the min and max
bounds of that bin, with \code{bin_count} being the count of individuals in that
bin. Note that \code{bin_count} can be non-integer, which, in particular, happens
when integer counts are scaled by effort. An extra column may well be
\code{species} identification, though this is not needed for the analysis (extra
columns are preserved in the output). The bin with the smallest \code{bin_min}
value and the bin with the largest \code{bin_max} value must both have non-zero
\code{bin_count} unless you explicitly specify \code{x_min} and/or \code{x_max} as appropriate.
}}

\item{x_min}{minimum value of data to fit the PLB distribution to. If \code{NULL}
(the default) then it is set to the minimum bin break of the lowest
bin (which must have non-zero \code{bin_count} as mentioned above). If not \code{NULL}
then the fitting is restricted to values greater than or equal to
\code{x_min}, which for the MLEbins method is the first full bin equal to or above \code{x_min}
(i.e. first bin with \code{bin_min >= x_min}). Similarly for \code{x_max} (fitting
is restricted to including the largest bin for which \code{bin_max <= x_max}).}

\item{x_max}{maximum value of data to fit the PLB distribution to. If \code{NULL}
(the default) then it is set to the maximum bin break of the highest bin
(which must have non-zero \code{bin_count} as mentioned above.)}

\item{b_start}{for the MLEbins method, the starting estimate for numerical
search for the MLE; since there is no analytical value that can be
calculated. Change this if you run into numerical issues.}

\item{b_vec}{vector of values for the confidence interval calculation, to be
used as the \code{vec} argument of \code{calc_mle_conf()}}

\item{b_vec_inc}{increment value for the confidence interval calculation, to be
used as the \code{vec_inc} argument of \code{calc_mle_conf()}}
}
\value{
\code{list} object of class \code{size_spectrum_mlebins}, such that we can
automatically plot it with \code{\link[=plot.size_spectrum_mlebins]{plot.size_spectrum_mlebins()}}, with objects
\itemize{
\item \code{b_mle} maximum likelihood estimate of $b$
\item \code{b_conf} vector giving 95\% confidence interval for $b$
\item \code{data} the original \code{data.frame} \code{dat}, arranged by
the increasing values of \code{bin_min}, and also has the columns
\itemize{
\item \code{count_gte_bin_min} total count of values in bins for which \code{bin_min}
\eqn{\geq} the value of \code{bin_min} for this row
\item \code{low_count} total count of values in bins for which \code{bin_min}
\eqn{\geq} the value of \code{bin_max} for this row, so the lowest possible count of
values above this bin
\item \code{high_count} total count of values in bins for which \code{bin_max}
\eqn{\geq} the value of \code{bin_min} for this row, so the highest possible count of
values above this bin.
Note that if \code{x_min} and/or \code{x_max}
are prescribed such that some data are not included in the fit (e.g. \code{x_min}
is greater than the \code{bin_max} of the smallest bin), then they are omitted in \code{data}.
}
\item \code{x_min} the \code{x_min} value used for the fitting
\item \code{x_max} the \code{x_max} value used for the fitting
\item \code{method} to describe the fitting method used, in this case \code{MLEbins}
}
}
\description{
The MLEbins method calculates the maximum likelihood estimate
of the size-spectrum exponent, $b$, when the data are collected as lengths and
we have species-specific length-weight coefficients. So individual body masses
are not available. Or, if you have binned body masses but the bins overlap,
then you also need to use MLEbins. The method is described in our MEPS
paper.
}
\examples{
\dontrun{
# See the rendered vignette
# https://andrew-edwards.github.io/sizeSpectraFit/vignettes/fit-data-mlebins.html
# for a worked example.
}

}
