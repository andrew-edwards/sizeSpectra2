% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/determine-xmin-based-on-hist.R
\name{determine_xmin_based_on_hist}
\alias{determine_xmin_based_on_hist}
\title{Given histogram object of counts and bins (and maybe density) , determine the mode and return min of that bin as x_min}
\usage{
determine_xmin_based_on_hist(h)
}
\arguments{
\item{h}{histogram object, e.g. from running \code{\link[=make_hist]{make_hist()}} on a vector, or
\code{\link[=make_hist_for_binned_counts]{make_hist_for_binned_counts()}} or
\code{\link[=make_hist_for_binned_counts_mlebin]{make_hist_for_binned_counts_mlebin()}} on a data.frame for MLEbins or MLEbin
method, respectively.}
}
\value{
x_min to use to fit ISD, and to plot the histogram to show the full
data and the mode.
}
\description{
Given histogram object of counts and bins (and maybe density) , determine the mode and return min of that bin as x_min
}
\examples{
\dontrun{
sim_vec_binned_2 <- sim_vec_binned
sim_vec_binned_2[1, "bin_count"] <- 100  # lower count for first bin
hh <- make_hist_for_binned_counts_mlebin(sim_vec_binned)
x_min <- determine_xmin_based_on_hist(hh)
x_min

# The above gets calculated automatically within:
res_binned_2 <- determine_xmin_and_fit_mlebin(sim_vec_binned_2)
plot(res_binned_2)
}
}
\author{
Andrew Edwards
}
