% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/determine-xmin-and-fit-mlebins.R
\name{determine_xmin_and_fit_mlebins}
\alias{determine_xmin_and_fit_mlebins}
\title{Given data for MLEbins calculation, with species-specific weight bins,
determine a common binning and use that to calculate \code{x_min} and then fit
using MLEbins.}
\usage{
determine_xmin_and_fit_mlebins(
  dat_for_mlebins,
  bin_width = 1,
  bin_start = 0,
  x_min = NULL,
  ...
)
}
\arguments{
\item{dat_for_mlebins}{tibble of data in the format required for fitting
using MLEbins method; see \code{\link[=fit_size_spectrum_mlebins]{fit_size_spectrum_mlebins()}}.}

\item{bin_width}{numeric bin width for the data set.}

\item{bin_start}{numeric value for the first bin to start at; if \code{NULL} then
set to the next multiple of \code{bin_width} below \code{min(x)}. If \code{bin_width = 1} then this will
be the integer below \code{min(x)} (i.e. \code{min(x) \%/\% 1}). If,
say, \code{min(x) = 10.6} and \code{bin_width = 0.5} then \code{bin_start = 10.5}.}

\item{x_min}{minimum value of data to fit the PLB distribution to. If \code{NULL}
(the default) then it is determined by the histogram method (see Quevedo
et al. 2026), by binning the data (based on each \code{bin_min}) in
\code{\link[=make_hist_for_binned_counts]{make_hist_for_binned_counts()}}, determining the mode in
\code{\link[=determine_xmin_based_on_hist]{determine_xmin_based_on_hist()}}, and setting \code{x_min} to be the minimum
\code{bin_min} that is above the minimum of that modal bin. If not \code{NULL}
then the fitting is restricted to values greater than or equal to
\code{x_min}, which for the MLEbins method is the first full bin equal to or above \code{x_min}
(i.e. first bin with \code{bin_min >= x_min}).}

\item{...}{arguments passed onto \code{\link[=fit_size_spectrum_mlebins]{fit_size_spectrum_mlebins()}}}
}
\value{
list of class \code{determine_xmin_and_fit_mlebins} for plotting, containing
\itemize{
\item \code{mlebins_fit} object of class \code{size_spectrum_mlebins} from using MLEbins
method; see \code{\link[=fit_size_spectrum_mlebins]{fit_size_spectrum_mlebins()}}
\item \code{h} histogram object, as used to determine \code{x_min}
}
}
\description{
See
\href{https://andrew-edwards.github.io/sizeSpectraFit/vignettes/fit-data-mlebins.html}{fit-data-mlebins vignette}
for explanation.
}
\examples{
\dontrun{
# See fit-data-mlebins vignette, rendered at
#  https://andrew-edwards.github.io/sizeSpectraFit/vignettes/fit-data-mlebins.html
}
}
\author{
Andrew Edwards
}
